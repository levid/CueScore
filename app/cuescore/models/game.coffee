class $CS.Models.Game
  constructor: (@owner, 
                @course, 
                @playingFor='brag', 
                @scoringFormat='low_net') ->
                  
    console.log  "game model"

  serialize: ->
    
  deserialize: (data) ->
    
  save: ->
    
  resume: ->
    
  dataForSubmit: () ->
    
  submit: (error) ->