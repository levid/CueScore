$CS.Views.Dashboard.createMainWindow = (options={}) ->
  window = Ti.UI.createWindow(options)
  window
  
$CS.Views.Dashboard.createMainView = (options={}) ->
  view = Ti.UI.createView(options)
  view